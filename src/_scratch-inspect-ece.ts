// @ts-nocheck
// (Leftover scratch file from a one-off inspection — the sandbox couldn't
// reach the Atlas cluster to run it, so the same check was done via the
// live REST API instead. Safe to delete; kept empty so it doesn't confuse
// anyone who opens it.)
